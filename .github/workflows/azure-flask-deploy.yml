name: Deploy GA Flask App to Azure

on: # DISABLED - Use azure-webapp-deploy.yml
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: [ 'github-deployment/**' ]

jobs:
  deploy:
    runs-on: # DISABLED - Use azure-webapp-deploy.yml ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: # DISABLED - Use azure-webapp-deploy.yml '3.11'
        
    - name: Install dependencies
      run: |
        cd github-deployment
        pip install -r requirements.txt
        
    - name: Deploy to Azure
      uses: azure/webapps-deploy@v2
      with:
        app-name: ga-hwaffmb0eqajfza5
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: './github-deployment'
